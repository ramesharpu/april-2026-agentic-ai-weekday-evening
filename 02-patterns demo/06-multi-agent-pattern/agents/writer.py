class WriterAgent:

    def write(self, research):
        return (
            "AI is useful because it automates repetitive tasks, "
            "improves decision-making, and learns from data."
        )